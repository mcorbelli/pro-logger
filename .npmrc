# When true, any missing non-optional peer
# dependencies are automatically installed.

auto-install-peers=true

# If this is enabled, commands will fail if there
# is a missing or invalid peer dependency in the tree.

strict-peer-dependencies=true